#include <bits/stdc++.h>
#define For(i,a,b) for(int i=a;i<b;i++)
#define rev(c) reverse(c.begin(),c.end())
#define ll long long
using namespace std;
int main(){
	int t; cin>>t;
	while(t--){
		int n,k;
		cin>>n>>k;
		vector<int>a;
		For(i,0,n) cin>>a[i];
		cout<<endl;	
	}
}
